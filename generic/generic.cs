//제네릭 : Cup<T : type>를 Cup of T로 발음하여 형식 매개 변수인 T에 따른 Cup 클래스의 개체 생성
// List<T> 는 T자리에 type을 정해서 여려개의 값을 넣어서 사용 할 수 있도록 해준다.
// Dictionary<T> 는 T자리에 type을 지정 두가지 타입을 정해서 key. value형식으로 사용가능하다.
using System.Collections;
using System.Globalization;

class ListT 
{ 
    List<int> numbers = new List<int>();
    static void Main()
    { 
     
    }
}

